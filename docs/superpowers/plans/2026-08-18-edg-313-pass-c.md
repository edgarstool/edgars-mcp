# EDG-313 PASS-C Implementation Plan

**Goal:** Live-prove ChatGPT DEV → Logto → `/chatgpt-honcho` → Honcho staging remember/recall.

**Architecture:** Add a feature-gated MCP path to the current edgars-mcp HTTP server. Validate Logto bearer tokens against Logto `userinfo`; proxy only two memory tools to isolated Honcho staging at `127.0.0.1:18000`. Existing `/mcp`, Cloudflare Access behavior, production Honcho `:8000`, and real `edgar-team` remain untouched.

**Constraints:**
- DEV only; no production connector cutover.
- No token/secret persistence or logging.
- Synthetic workspace only; cleanup must be verified.
- Only `recall_edgar_memory` and `remember_edgar_memory` are exposed.

## Tasks
1. Add failing tests for protected-resource metadata, Logto auth, two-tool list, remember/recall, and path isolation.
2. Implement the minimal feature-gated endpoint and Logto userinfo validation.
3. Implement Honcho staging conclusion write/query with required observer/observed filters.
4. Run focused tests, full HTTP regression tests, syntax checks, and live origin smoke.
5. Deploy DEV route, verify public discovery/auth behavior, then run ChatGPT DEV OAuth + tools/list + synthetic remember/recall.
6. Commit/push branch, update EDG-313 with evidence, and read it back before claiming PASS-C.