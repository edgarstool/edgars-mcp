## MiniMax Archive

這裡保留 `MiniMax` 舊版 handler，方便之後回查或拆成獨立 plugin / 獨立 repo。

- 封存日期：2026-07-15
- 原因：德德目前已不使用 MiniMax，`edgars-mcp` 主 server 不再預設載入 `mmx_*` 工具。
- 影響：`server_http.py` 主工具清單、dispatch、README 與 Doppler 說明都已同步拔除。

這一區只作歷史保留，不再視為主入口。
