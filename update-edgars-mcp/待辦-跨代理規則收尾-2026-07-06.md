# 跨代理規則收尾待辦（2026-07-06）

目的：
把這次查到的多代理 instruction / 規則分裂問題正式收斂，不要之後又回頭重查。

## P1 先做

- 建立正式對照表：
  - 列出 `Warp / Cursor / Claude / Factory / Hermes / Kilo / Devin / Droid`
  - 各自實際讀哪個入口檔
  - 哪些是全域設定，哪些只是某個 repo 的 `AGENTS.md`
- 清理 Claude 全域規則漂移：
  - `C:\Users\EdgarsTool\.claude\CLAUDE.md`
  - 目前仍殘留大量 `D:\Agent-KB` 舊路徑
  - 要改成現在的 `G:\Agent-KB` / `V:\projects` 正式地形
- 處理 Devin 舊路徑與權限漂移：
  - `C:\Users\EdgarsTool\AppData\Roaming\Devin\config.json`
  - 目前仍殘留 `D:\Agent-KB` 相關權限
  - 要改成現行正式路徑

## P1 安全補強

- 檢查 Devin 相關設定是否仍保留舊敏感值注入方式
- 若敏感憑證仍有效，安排輪替 / rotate
- 後續紀錄中只寫「要輪替哪一類 secret」，不要把 secret 值寫進任何文件

## P2 收斂

- 決定 Cursor 的正式規則注入點：
  - 目前只看到 `settings.json`
  - 尚未看到 Cursor 使用者層專屬 `AGENTS.md`
- 決定 Factory / Droid 的正式規則注入點：
  - 目前未看到獨立 `agent.md`
  - 需要確認是 ACP agent、extension config，還是根本沒有本機規則入口
- 釐清 Kilo 的正式策略：
  - 目前偏向 `skills` 驅動
  - 需要決定是否要補一份全域 instruction 入口

## P2 邊界整理

- Warp 目前看到的 `AGENTS.md` 是 `hermes-agent` worktree 內的 repo-local 規則
- 需要明確標記：
  - 這不是 Edgar 全域規則
  - 不能拿它當所有代理共用母本

## 完成標準

- 有一份正式對照表
- Claude / Devin 不再指向 `D:\`
- 各工具的全域入口與 repo-local 入口已分清
- 後續代理不會再因為吃到不同份 instruction 而各說各話
